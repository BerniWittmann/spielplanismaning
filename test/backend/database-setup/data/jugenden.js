const ids = require('./ids');

const data = [{
    _id: ids.jugend,
    gruppen: ids.gruppen,
    teams: ids.teams,
    color: 'blau',
    veranstaltung: ids.veranstaltung
}];

module.exports = {
    data: data
};