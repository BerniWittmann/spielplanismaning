const ids = require('./ids');

const data = [{
    _id: ids.veranstaltung,
    name: 'Event',
    bildUrl: '',
    spielModus: 'normal',
    printMannschaftslisten: true
}];

module.exports = {
    data: data
};